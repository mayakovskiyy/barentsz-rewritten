# barentsz-rewritten
A simple password generator written in Swift.

# Parsable command
Just use `bsz {symbols}` to generate a password. `--copy` to copy password, `--save` to save password and `--exclude <symbol>` to exclude a symbol.

# KISSed
Simple and not bloated

### Made by Danketsu Studio with love.
2026, Danketsu Studio©
